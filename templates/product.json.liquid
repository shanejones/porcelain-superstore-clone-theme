{% layout none %}

{
    "metafields": {
      "size_dimensions": "{{ product.metafields.product.size_dimensions | escape }}",
      "price_per_meter_squared": "{{ product.metafields.product.price_per_meter_squared | escape }}"
    }
}